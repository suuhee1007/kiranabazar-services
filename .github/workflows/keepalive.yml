name: Keep Render App Awake

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Render service
        run: |
          curl -fsS -m 20 "https://kiranabazar-services.onrender.com/health" || true
